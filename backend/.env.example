# Copy this file to .env and fill in your values
# Never commit .env to git

PORT=8000
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/nomination
JWT_SECRET=replace_with_a_long_random_string
GROQ_API_KEY=your_groq_api_key_from_console.groq.com
MONGO_TLS_ALLOW_INVALID_CERT=false

# ── Email / SMTP settings (for deadline reminder emails) ──────────────────────
# Leave blank to disable emails (server still runs fine without these)  # "From" address shown in the email       # Used in the "Open System" button in emails
EMAIL_HOST=smtp.resend.com                     
EMAIL_PORT=587                                                          
EMAIL_USER=resend                                         
EMAIL_PASS=re_QvRJndqF_L8q4cenukhHJMZRpbo28Vjdm                                                        
EMAIL_FROM=onboarding@resend.dev                                  
APP_URL=http://localhost:3000 